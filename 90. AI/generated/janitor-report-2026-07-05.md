# Janitor Report — 2026-07-05

## What Looks Good

- **AI system files all present.** `me.md`, `memory.md`, `memory-changelog.md`, `skill-map.md`, `startup-prompt.md`, `system-changelog.md`, `vault-map.md` — all accounted for.
- **All 10 skills, commands, and Claude Code commands present.** `90. AI/skills/`, `90. AI/commands/`, and `.claude/commands/` are complete with matching coverage.
- **No stale "AIOS" or "Indexes" references in user content.** The only hits are in janitor skill docs (checking for them) and `memory.md` (documenting the rename). Clean migration.
- **Main notes are well-connected.** Both `Linux Shell` (12 wikilinks) and `Linux Commands for Everyday Use` (12 wikilinks) link upstream, downstream, and sideways. No orphan main notes.
- **Linux MOC is fresh.** Updated earlier today with proper structure, subtopics, and honest gap tracking.
- **All 5 templates exist** in `99. Templates/`.
- **No stuck inbox notes.** The oldest inbox items are from June 28 (1 week ago). Nothing older than 2 weeks.
- **Tags are being used.** Every tag with content is linked from at least one note (except `Command Line`).
- **No AI drafts found in `03. Main Notes/`.** All generated content is correctly in `90. AI/generated/`.

## Problems Found

### 1. Six completely empty tag notes (0 bytes)

All tag files in `04. Tags/` contain no content:

| Tag file | Size |
|----------|------|
| `Command Line.md` | 0 B (also unused — no notes link to it) |
| `DevOps.md` | 0 B |
| `Filesystem.md` | 0 B |
| `Linux.md` | 0 B |
| `Linux Basics.md` | 0 B |
| `Shell.md` | 0 B |

Empty tag notes defeat their purpose — they're meant to be link hubs with a short explanation of what the tag means and how it connects. Right now they're invisible in the graph.

### 2. Two empty inbox notes

- `01. Inbox/2026-07-05 -.md` — 0 bytes, odd filename (space before `.md`, no topic name)
- `01. Inbox/Linux cd command.md` — 0 bytes, created today, nothing written

### 3. Five inbox notes with content awaiting processing

These all have draft content and should move to `03. Main Notes/`:

| Note | Date | Has Content? | Format Issues |
|------|------|-------------|---------------|
| `Common Linux Directories.md` | Jun 28 | ✅ 759 B | Uses old template (`type: rough-note`, `Topic:`) |
| `Linux ls command.md` | Jun 28 | ✅ 569 B | Draft, links to non-existent `[[cd command]]` |
| `SSH (Secure Shell).md` | Jul 5 | ✅ 1.2 KB | Well-structured, ready for promotion |
| `The pwd command.md` | Jun 28 | ✅ 636 B | Draft, empty connection sections |
| `The Root directory is the top of the Linux filesystem.md` | Jun 28 | ✅ 870 B | Uses very old template (`type: Atomic Note`, `cssclasses`, `Topic:`) |

### 4. Overlapping/duplicate content in inbox

- **`Common Linux Directories`** and **`The Root directory is the top of the Linux filesystem`** overlap significantly. Both cover the Linux directory tree (`/`, `/bin`, `/home`, `/tmp`, `/usr`, `/var`, `/opt`). The Root note explains the concept ("everything starts with `/`"); the Common Directories note lists the same paths with one-line descriptions. These should be merged or one should focus on the root concept and the other on the directory catalog.
- **`Linux ls command`** (inbox) is referenced as `[[Linux ls command]]` from the updated `Linux Commands for Everyday Use` (main note). If the inbox version is promoted, the link resolves. But the inbox version currently links to a non-existent `[[cd command]]` (should be `[[Linux cd command]]`).

### 5. 24 broken wikilinks

**Real content gaps** (notes that should exist but don't):

From `03. Main Notes/` and `05. MOCs/` (the ones that matter most):
- `[[Linux cat command]]`, `[[Linux cd command]]`, `[[Linux grep command]]` — individual command notes
- `[[Bash Shell]]`, `[[Zsh Shell]]` — shell variants
- `[[GUI - Graphic User Interface]]` — contrasting concept
- `[[SSH (Secure Shell)]]` — note exists in inbox but not yet in main notes

From `01. Inbox/`:
- `[[cd command]]` in `Linux ls command.md` — should be `[[Linux cd command]]`

**Example/demonstration links in AI files** (not real gaps — these are sample links in skill/command documentation):
- `[[Kubernetes]]`, `[[Photography]]`, `[[Learning]]`, `[[Tag]]`, `[[TagName]]`, `[[DevOps MOC]]` — used as examples in skills, CLAUDE.md, and command prompts
- `[[Pods are temporary in Kubernetes]]`, `[[Kubernetes Services give pods stable network access]]`, `[[Deployments manage desired state]]` — example notes in `sherpa.md`

**From ai-conversations** (14 broken links in one file — see item 7 below):
- Various Linux and shell concept links generated by an AI conversation recap

### 6. Outdated MOC template

`99. Templates/MOC.md` doesn't match the current MOC standard used by `/update-moc`:

| Template says | Standard says |
|---------------|---------------|
| `Topic:` | `topic:` |
| `labels/tags:` | `tags:` |
| `## Source notes` | `## Best Source Materials` |
| `## Projects / labs` | `## Open Questions`, `## Notes To Create Next`, `## Related MOCs` |
| No `domain` field | Has `domain:` |

The template would produce MOCs in the old style that the janitor would then flag.

### 7. Unlisted `90. AI/ai-conversations/` folder

Contains one file: `ActiveNote_Recap_Request@20260705_190306.md` (14 KB). This folder is not mentioned in `vault-map.md`, `CLAUDE.md`, or `AGENTS.md`. The file has 14 broken wikilinks (Linux concepts, shell topics) that look like AI-generated suggestions rather than actual vault content. This appears to be an external tool integration (ActiveNote?) that wrote into the vault.

### 8. Unlisted `copilot/` folder at vault root

Contains 15 GitHub Copilot custom prompt files. Not part of the documented vault structure. Not harmful, but if it's actively used it should be noted somewhere; if it's abandoned, it's clutter.

### 9. Four skill backup files in `90. AI/skills/`

- `memory-curator.backup-20260704-1241.md`
- `memory-curator.backup-20260704-1244.md`
- `weekly-review.backup-20260704-1241.md`
- `weekly-review.backup-20260704-1244.md`

These look like pre-edit backups from the July 4 system overhaul. They're not harmful but they clutter the skills folder. If the current versions are stable, these can be deleted or moved to `90. AI/history/`.

### 10. All six Source Materials subfolders are empty

`Articles/`, `Books/`, `Courses/`, `Documentation/`, `Podcasts/`, `Videos/` — zero notes across all six. Not a problem per se (it means no source material has been processed yet), but if Pedro has been learning from sources, there's a backlog to capture.

### 11. `Command Line` tag is unused

The file exists at `04. Tags/Command Line.md` but is empty (0 bytes) and no notes link to `[[Command Line]]`. Compare with `Shell` which is linked from 2 notes. Either merge into `Shell` or populate with an explanation of how it differs.

### 12. Inconsistent frontmatter conventions across notes

The vault appears mid-migration between frontmatter styles:

| Convention | Used in |
|------------|---------|
| `domain:` + `track:` + `tag:` (newer) | `Linux Shell`, `Linux Commands for Everyday Use`, most inbox drafts |
| `domain:` + `Topic:` + `labels/tags:` (older) | `Common Linux Directories`, `The Root directory...` |
| `type: moc` + `Topic:` + `labels/tags:` | MOC template, old Linux MOC |
| `type: rough-note`, `type: Atomic Note` | Older inbox notes |

This isn't breaking anything but it means templates produce inconsistent output.

## Suggested Fixes

### Quick wins (read-only verification, then apply)

1. **Delete the two empty inbox notes:** `2026-07-05 -.md` and `Linux cd command.md` (or populate `Linux cd command.md` if it was meant to be written).

2. **Update the MOC template** to match the current standard (`topic:`, `tags:`, `## Best Source Materials`, `## Open Questions`, `## Notes To Create Next`, `## Related MOCs`).

3. **Delete or archive the 4 skill backup files** in `90. AI/skills/` (move to `90. AI/history/` if they have archival value).

### Content work

4. **Process the 5 inbox notes** — promote `SSH (Secure Shell)`, `The pwd command`, and `Linux ls command` to main notes. Merge `Common Linux Directories` and `The Root directory...` into one note (or split cleanly: one on the root concept, one on the directory catalog).

5. **Populate the 6 empty tag notes** — at minimum add a one-liner explanation to each. Priority: `Linux`, `Shell`, `DevOps` (most linked-to).

6. **Fix the broken link in `Linux ls command`** — `[[cd command]]` → `[[Linux cd command]]`.

### Decisions needed

7. **`ai-conversations/` folder** — document it in vault-map.md if it's a permanent feature, or move the file to `90. AI/history/` if it was a one-off.

8. **`copilot/` folder** — document or remove.

9. **`Command Line` tag** — populate it or delete it (merge into `Shell`).

## Fixes That Need Pedro's Approval

- Deleting any files (empty inbox notes, backup files)
- Merging `Common Linux Directories` + `The Root directory...` into one note
- Updating the MOC template (changes what new MOCs look like)
- Handling the `copilot/` folder (was this intentional?)
- Handling the `ai-conversations/` folder (is ActiveNote an active integration?)
- Populating or deleting the `Command Line` tag
- Resolving frontmatter convention (pick one style and update all templates/notes to match)

## Do Not Touch

- `90. AI/memory.md` — guarded, changes via proposal only
- `90. AI/skills/*.md` (non-backup) — all 10 active skills are in use
- `90. AI/commands/*.md` — all 10 command prompts are referenced
- `.claude/commands/*.md` — all 10 Claude Code commands are working
- `03. Main Notes/Linux Shell.md` — well-structured, no issues
- `05. MOCs/Linux MOC.md` — freshly updated today

## Recommended Next Steps

1. **Start with the quick wins** — delete 2 empty inbox files and 4 backup files (~30 seconds, no risk)
2. **Update the MOC template** — aligns template with actual practice
3. **Process inbox notes** — run `/process-inbox` to get a structured proposal, then promote the 5 ready notes
4. **Populate top 3 tag notes** — `Linux`, `Shell`, `DevOps` — one sentence each is enough
5. **Decide on `copilot/` and `ai-conversations/`** — document or remove
6. **Create the missing command notes** — `Linux cd command`, `Linux cat command`, `Linux grep command` are the most-linked gaps
